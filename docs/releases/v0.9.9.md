# Release v0.9.9

**Тип:** patch release (v0.9.9)  
**Дата:** 2026-08-05  
**База:** `main` (after v0.9.8)

> CI stabilization after v0.9.8: agent-spike clippy on Linux + CI workflow
> permissions. Full notes: [CHANGELOG.md](../../CHANGELOG.md).

## Highlights (с v0.9.8)

| Change | Purpose |
|---|---|
| `system_proxy` clippy fix | Green `cargo clippy -D warnings` on Linux CI |
| `ci.yml` permissions | Satisfy code scanning / least-privilege Actions |

No new agent features. Use this tag for images/packages if CI green on the
release commit matters more than staying on `v0.9.8` (which failed clippy).

## Upgrade from 0.9.8

No API or config changes. Rebuild/redeploy binaries/images only.

## Release tag

```bash
git tag -a v0.9.9 -m "BSDM-Proxy v0.9.9"
git push origin v0.9.9
```
