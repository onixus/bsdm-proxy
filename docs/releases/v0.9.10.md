# Release v0.9.10

**Тип:** patch release (v0.9.10)  
**Дата:** 2026-08-05  
**База:** `main` (after v0.9.9)

> Pilot polish + Phase C **fleet packaging scaffolding**. Full notes:
> [CHANGELOG.md](../../CHANGELOG.md).

## Highlights (с v0.9.9)

| Change | Purpose |
|---|---|
| Admin Console in proxy image | Day-1 `/admin/` without host SPA mount |
| Search CORS (cache-indexer) | Browser Admin `:9090` → Search `:8080` |
| L1 cache HIT framing fix | Stable keep-alive after cache hits |
| dns-sinkhole zone dir perms | Non-root can load RPZ |
| Agent fleet (Intune/GPO/Jamf) | Silent install + MDM scaffolding (unsigned) |
| Admin defaults `HTTP_PORT=3128` | Align UI export with proxy default |

## Not included

- Apple notarization / Authenticode signing (customer pipeline)
- New experimental modules (still frozen)

## Upgrade from 0.9.9

1. Rebuild images (`docker compose build` or GHCR tag `v0.9.10`).
2. Optional: drop host mount of `admin-console/dist` — SPA is in the image.
3. Agent fleet: use `packaging/agent/fleet/` + silent installers; sign packages
   before production.

No breaking control-plane API changes.

## Release tag

```bash
git tag -a v0.9.10 -m "BSDM-Proxy v0.9.10"
git push origin v0.9.10
```
