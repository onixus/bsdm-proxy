# Hybrid load-test results

Machine-written reports from
[`scripts/run-hybrid-load-test.sh`](../../../scripts/run-hybrid-load-test.sh).

| File | Meaning |
|---|---|
| `latest.md` | Copy of the most recent run on this machine |
| `YYYYMMDDTHHMMSSZ.md` | Immutable run snapshot (UTC timestamp) |

Commit selected runs that document a pilot or CI baseline. Skip noisy laptop
runs with flaky public upstreams.

Methodology: [load-test-selective-mitm.md](../load-test-selective-mitm.md).
